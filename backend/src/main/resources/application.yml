spring:
  application:
    name: meetr
  datasource:
    url: jdbc:mysql://localhost:3306/meetr?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: meetr
    password: meetr123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false
  sql:
    init:
      mode: never

server:
  port: 8080
