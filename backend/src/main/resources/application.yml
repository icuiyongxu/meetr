spring:
  application:
    name: meetr
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/meetr?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123qwe!@#QWE
  sql:
    init:
      mode: always
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  mail:
    host: smtp.example.com
    port: 587
    username: noreply@example.com
    password: your-smtp-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.meetr.domain.entity
  configuration:
    map-underscore-to-camel-case: true
    call-setters-on-nulls: true
    jdbc-type-for-null: 'NULL'

pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql

server:
  port: 8080

# JVM 启动参数需要加: -Duser.timezone=Asia/Shanghai
# 或者在运行配置里设置: -Duser.timezone=Asia/Shanghai

mail:
  host: smtp.163.com
  username: decocalc@163.com
  password: VYuVXh2yE32zYv9y
  default-encoding: UTF-8
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
          required: true

meetr:
  jwt:
    secret: meetr-super-secret-key-change-in-production
    expiration: 86400000
